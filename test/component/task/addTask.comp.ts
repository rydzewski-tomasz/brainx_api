import { initFullEnv } from '../../common/setup/initFullEnv';

describe('Add task component test', () => {
  initFullEnv();

  it.skip('GIVEN valid body WHEN addTask THEN return 200 status', async () => {
    // GIVEN
    // WHEN
    // THEN
  });

  it.skip('GIVEN valid body WHEN addTask THEN save task on db', async () => {
    // GIVEN
    // WHEN
    // THEN
  });
});
